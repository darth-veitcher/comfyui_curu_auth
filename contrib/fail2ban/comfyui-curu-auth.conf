# Copy to /etc/fail2ban/filter.d/comfyui-curu-auth.conf
#
# Verify the regex against your own log lines before relying on it:
#   fail2ban-regex /path/to/comfyui/output.log comfyui-curu-auth.conf
[Definition]
failregex = ^.*comfyui_curu_auth: authentication failure from <HOST>.*$
ignoreregex =
